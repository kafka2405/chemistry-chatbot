# Bước 1: Dùng file này trước (chưa có SSL)
# Sau khi chạy certbot thành công, thay bằng nội dung bên dưới (có SSL)

server {
    listen 80 default_server;
    server_name trolyhoahoc.online _;

    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Frontend (Vue static files)
    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Backend API + Session cookie
    location /api/ {
        proxy_pass http://backend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


# ============================================================
# SAU KHI CÓ SSL: Thay toàn bộ nội dung trên bằng phần dưới
# (Bỏ comment tất cả các dòng bên dưới, xóa phần trên)
# ============================================================

# server {
#     listen 80;
#     server_name trolyhoahoc.online;
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl;
#     server_name trolyhoahoc.online;
#
#     ssl_certificate /etc/letsencrypt/live/trolyhoahoc.online/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/trolyhoahoc.online/privkey.pem;
#
#     # Frontend
#     location / {
#         proxy_pass http://frontend:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#
#     # Backend API
#     location /api/ {
#         proxy_pass http://backend:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
